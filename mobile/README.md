# EduMind mobile

- **PWA (recommended for pilots):** use the Vercel URL → browser → Add to Home Screen. See `docs/MOBILE_INSTALL_GUIDE.md`.
- **Android APK (Capacitor):** see `docs/ANDROID_APK.md` and `frontend/capacitor.config.json`.
